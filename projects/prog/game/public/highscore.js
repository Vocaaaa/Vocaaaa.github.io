const btn = document.getElementById("button");

btn.addEventListener("click", reload);

function reload() {
    window.location.replace("index.html");
    return
  }

const scoreCounter = document.getElementById("last-round");

console.log = score;